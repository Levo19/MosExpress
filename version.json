{ "version": "2.8.63", "build": "Banner de version en consola: imprime 'ME cargado v2.8.63' en cada carga + compara con el server y avisa si hay una mas nueva (igual que MOS). Asi siempre se ve la version y se detectan updates pendientes." }
