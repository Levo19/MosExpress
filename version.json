{
 "version": "2.8.106",
 "build": "2.8.106 (companion 100x fixes): XSS cerrado (nombre escapado), overlay solicitante no se duplica (guard), overlay de aprobar se auto-cierra a los ~2min (no bloquea pantalla), aprobar-falla usa cooldown (no re-pop storm), hold-to-confirm con guard de reentrancia + touchcancel, callbacks de poll envueltos en try/catch. Todo fail-safe, cero-GAS."
}
